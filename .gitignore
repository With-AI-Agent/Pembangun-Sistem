__pycache__/
*.pyc
.DS_Store

# Artefak generated oleh tools/ — dibuat ulang lewat backup_verify.py / build_template.py,
# sengaja TIDAK di-commit supaya repo tidak bengkak. Rujukan ke path ini di
# _meta/SYSTEM_MANIFEST.md dan _meta/TEMPLATE_RELEASE.md karena itu muncul sebagai
# WARNING (bukan error) di tools/validate_repo.py — itu perilaku yang diharapkan.
_meta/_internal/backups/
_meta/_internal/template_clean/
_meta/_internal/template_clean.zip

# Hasil `tools/pack_repo.py` (paket repo mandiri per sistem). Putusan pemilik
# 7 Sep 2026: pack TIDAK di-commit ke master — master adalah satu-satunya sumber
# kebenaran dan pack selalu bisa dibangkitkan ulang dari sha yang tercatat di
# PAKET_REPO.md. Lihat _meta/PAKET_REPO_MANDIRI.md.
dist/
*-repo-*.zip

# M-12 (audit meta 5 Sep 2026): state UI lokal Obsidian berubah tiap buka vault —
# noise diff & sumber konflik sync. Pengaturan vault lainnya tetap ikut repo.
.obsidian/workspace.json
