# LOG_SESI — 2026-09-08_6 — PR A folder sistem mandiri

## Keadaan Sesi

- **Status:** CLOSED
- **Keadaan:** `CLOSED` — **KOREKSI 19 Sep 2026 (append):** field `- **Keadaan:**` ditambahkan karena `tools/validate_repo.py` kini menjaganya (usulan hakim putaran 5 PR #74, temuan #6: header Keadaan Sesi bisa basi/hilang tanpa ada yang protes). Nilainya **disalin dari field `- **Status:**` lama di berkas ini**, bukan diputuskan baru; teks lama tidak dihapus.
- **Scope:** tools dan dokumen meta; tidak menyunting isi `sistem-konten-kreator/` atau `sistem-presentasi/`.
- **Keputusan pemilik yang dipakai:** tools/pack_repo.py pensiun; folder sistem menjadi deliverable; yang disalin adalah yang dipakai; rujukan operasional ke `_meta/` atau `tools/` di dalam folder sistem harus punya salinan berlabel.
- **Batas yang sengaja dijaga:** rujukan keluar folder di dua sistem domain lama dilaporkan oleh `tools/check_selfcontained.py`, bukan ditambal di PR A.

## Kronologi

### 2026-09-08 — Orientasi

Membaca log OPEN dari yang terlama, lalu `_meta/00_CARA_KERJA_META.md`, `_meta/PAKET_REPO_MANDIRI.md`, dan `_meta/PROTOKOL_REVIEW_INDEPENDEN.md`. Keputusan pemilik pada prompt PR A langsung dieksekusi tanpa meminta persetujuan ulang.

### 2026-09-08 — Perubahan inti

- tools/pack_repo.py dihapus dengan `git rm`; jejak pensiunnya ditulis di `_meta/PAKET_REPO_MANDIRI.md` bersama commit terakhir sebelum pensiun.
- `_meta/PAKET_REPO_MANDIRI.md` ditulis ulang menjadi protokol folder mandiri: dua kelas rujukan, label salinan turunan, dan gerbang `tools/check_selfcontained.py`.
- `tools/checkpoint_core.py` dibersihkan dari profil repo dan daftar putih rujukan-absen; yang tersisa adalah kewajiban statis, parser fail-closed, dan definisi dokumen aktif.
- `tools/validate_repo.py` dibersihkan dari mode mandiri; kewajiban validator sistem dan aturan larangan angka korpus di sel Bukti dipertahankan.
- `tools/check_selfcontained.py` ditambahkan sebagai alat master. Jika alat disalin ke folder tanpa `_meta/`, ia keluar dengan pesan alat master dan exit 2.
- `tools/review_prompt.py` ditambal: deteksi pin hanya pada definisi konstanta pin, `tools/review_prompt.py` menjadi ARBITER_PATHS eksplisit dengan alasan "pembangkit prompt pengadil", urutan baca memuat Markdown root, dan log penulis PR sendiri tidak memicu penyembunyian rumusan tetapi tetap dipointerkan.
- `tools/test_failure_injection.py` menghapus eksekusi P1–P3 lama dan menambahkan uji mutasi RP1–RP3 untuk tiga cacat review_prompt.
- `tools/build_template.py` menambahkan sistem-benih di hasil template, berisi validator sistem dan satu contoh salinan berlabel nyata.

### 2026-09-08 — Koreksi catatan PR lama

Catatan lama PR #24 tentang sengaja menulis rujukan pembangkit tanpa backtick karena packager belum mengenal alat master sudah tidak dipakai. Dokumen protokol review kini merujuk pembangkit dengan backtick karena berkasnya memang ada, dan pengecualian pengadil tidak lagi bergantung pada tabrakan label.


### 2026-09-08 — Verifikasi sebelum PR

Rangkaian verifikasi akhir dijalankan setelah perubahan kode dan dokumen. `tools/validate_repo.py` exit 0; warning tier yang tersisa menunjuk referensi packager lama di manifest dua folder sistem domain dan sengaja tidak diperbaiki di PR A karena scope tidak menyunting folder sistem. `tools/test_failure_injection.py` PASS. `tools/build_template.py` PASS dan smoke extract menjalankan validator, injeksi kegagalan, serta `tools/check_selfcontained.py --semua` pada sistem-benih. `tools/check_selfcontained.py --semua --report` menunjukkan dua sistem domain lama belum mandiri, sesuai batas PR B/C. Salinan alat mandiri tanpa `_meta/` juga mengembalikan pesan bahwa alat ini hanya untuk master.

### 2026-09-08 — Batas tidak dikerjakan

Tidak ada perubahan di `sistem-konten-kreator/` atau `sistem-presentasi/`. Rujukan keluar folder, self-prefix, dan jejak packager di dalam dua folder itu tetap merah sebagai pekerjaan PR B/C. Tidak ada klaim bahwa kedua folder tersebut sudah mandiri.
### 2026-09-08 — Koreksi format label salinan

Setelah verifikasi awal, format label ditegaskan bahwa sha adalah hash isi sumber master. `tools/check_selfcontained.py` kini membandingkan sha label dengan isi sumber master selain membandingkan badan salinan; `tools/build_template.py` menulis label sistem-benih dengan hash isi sumber. Rangkaian verifikasi akhir dijalankan ulang setelah catatan ini.

### 2026-09-08 — Koreksi prompt review untuk berkas dihapus

Setelah PR dibuat, keluaran prompt review memperlakukan alat yang dihapus seolah masih harus dibaca sebagai berkas hidup. `tools/review_prompt.py` disesuaikan agar berkas `tools/` yang sudah tidak ada dibaca sebagai diff penghapusan dan rujukan pensiunnya. Verifikasi akhir dijalankan ulang setelah koreksi ini.

### 2026-09-08 — Putaran 1 MERAH dikerjakan

Keputusan pemilik atas T-1, T-2, T-3, T-6, T-8, dan T-9 dikerjakan tanpa menambah scope lain: dua rujukan packager di manifest sistem dibuat provenance tanpa backtick; format diagnostik validator dikembalikan untuk baris angka; gate mandiri mendapat skenario mutasi untuk SELF-PREFIX, MISSING-LABELED-COPY, STALE-COPY, DERIVED-NO-LABEL, dan kewajiban baris kedua Perbedaan; check_selfcontained dan review_prompt masuk daftar alat inti; pemindai jendela-uji membaca status terakhir; log PR #24 dan PR #25 ditutup administratif atas izin pemilik. Evidence awal setelah commit 354809a: validator PASS dengan WARNINGS none; failure injection PASS; build template dan smoke ekstrak PASS; check_selfcontained tetap merah pada dua sistem domain lama sesuai batas PR A.
