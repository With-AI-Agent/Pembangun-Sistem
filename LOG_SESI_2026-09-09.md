# LOG_SESI — 2026-09-09 — PR A2: cakupan gerbang folder mandiri

## Keadaan Sesi (selalu segar — agent baru BACA INI DULU)

- **Keadaan:** `OPEN`
- **Branch:** `arena/01a0837b-pembangun-sistem`, basis `main` = `be1c287` (merge PR #26).
- **Scope:** `tools/` + dokumen meta (`_meta/`) + satu butir pegangan pengguna root. **Tidak** menyunting isi `sistem-konten-kreator/` maupun `sistem-presentasi/`.
- **Di mana kita sekarang:** empat perubahan cakupan pada `tools/check_selfcontained.py` dikerjakan sesuai putusan pemilik PR A2, norma diselaraskan, regresi baru diuji-mutasi, meta dinaikkan satu minor ke `1.11.0`. PR dibuka tanpa merge.
- **Sudah disepakati (dari pemilik, near-verbatim):** kerjakan langsung tanpa minta persetujuan untuk hal yang sudah diputuskan; empat perubahan dan tidak lebih; perilaku yang ditegakkan untuk dokumen aktif tidak dilonggarkan sedikit pun; jangan mengarang definisi baru dan jangan menyalin daftar glob ke dalam alat; jangan mengutip jumlah rujukan/dokumen di baris Log Evolusi maupun di body PR (AT-16); keluaran gerbang mandiri dikutip UTUH karena itu hasil kerja dua sesi berikutnya; jangan merge sendiri.
- **Terbuka:** review independen L1 dan keputusan merge milik pemilik. Daftar kerja dua sistem domain lama tetap milik PR B/C.
- **Fakta orientasi:** tidak ada `LOG_SESI_*.md` berkeadaan OPEN yang isinya masih hidup — log terbaru (`LOG_SESI_2026-09-08_6.md`, PR A) berstatus CLOSED, dan log-log 7–8 September yang header-nya `OPEN` semuanya sudah ditutup administratif di akhir berkas (PR #20–#26 MERGED, `gh pr list --state all` = 0 PR terbuka). Konteks PR A2 diambil dari `LOG_SESI_2026-09-08_6.md` + review PR #26 (temuan T-8) + keluaran alat apa adanya.

## Keputusan dan cara pengerjaan

1. **Cakupan = satu definisi bersama.** Alat memanggil `dokumen_aktif()` dari `tools/checkpoint_core.py` lewat helper `active_doc_rels()`; tidak ada daftar glob baru di dalam alat. Dokumen bukti/mentah tidak ditegakkan, rujukannya dicetak di bagian "rujukan historis (tidak ditegakkan)" saat `--report`.
2. **Sebutan area.** Token ber-backtick yang berakhir garis miring bukan kegagalan; dicetak di bagian "sebutan area". `SELF-PREFIX` bentuk direktori (mis. rujukan ke folder sendiri) tetap gagal karena butir 4 pemilik menyebut SELF-PREFIX tanpa pengecualian.
3. **Area master-only.** Definisi "area yang tidak boleh keluar dari master" diangkat ke `tools/checkpoint_core.py` sebagai `master_only_reason()` dan dipakai bersama oleh `tools/build_template.py` (verifikasi AT-10) serta alat gerbang. Alasan memilih mengangkat, bukan menyalin: dua daftar yang sama di dua alat adalah persis akar masalah v1.6.0. Perilaku build_template tidak berubah — verifikasi memakai penilaian yang sama, keluaran build + smoke ekstrak identik.
4. **Pesan untuk area master-only** berbunyi "tulis sebagai provenance tanpa backtick" dan tidak pernah menawarkan salinan berlabel; salinan yang terlanjur dibuat dari area itu menjadi temuan `MASTER-ONLY-COPY` dengan solusi menghapus salinan. Diperiksa di pohon nyata: kedua folder sistem punya nol salinan berlabel, jadi tidak ada salinan yang perlu dihapus.
5. **Ambiguitas butir 5(iii) pemilik diputuskan, tidak ditanyakan ulang:** "rujukan `_meta/` di dokumen aktif → tetap kegagalan" dibaca sebagai rujukan ke SATU BERKAS `_meta/…` di dokumen aktif (bentuk direktori sudah dinyatakan bukan kegagalan oleh butir 2, dan butir 4 menyatakan MISSING-LABELED-COPY berkas operasional tetap gagal). Dibaca sebagai bentuk direktori, kedua butir itu saling meniadakan; dibaca sebagai bentuk berkas, ketiganya konsisten dan menjadi tiga pembedaan yang diminta butir 6. SC8 memakukan tafsir ini.
6. **Penyelarasan norma di luar dua dokumen yang disebut pemilik** (satu baris masing-masing, dideklarasikan di body PR): `_meta/DEFINITION_OF_DONE.md` dan `PANDUAN_PENGGUNA.md` memuat ringkasan aturan yang sama; membiarkannya berarti menyisakan tabrakan norma-alat jenis T-8 di dua tempat lain.

## Bukti (dijalankan di HEAD kerja, urutan butir 7 pemilik)

- `python3 tools/validate_repo.py` → PASS, `WARNINGS: none`, exit 0.
- `python3 tools/test_failure_injection.py` → PASS 53 skenario; baris Jumlah di `_meta/FAILURE_INJECTION_TESTS.md` disalin dari keluaran alat.
- `python3 tools/build_template.py` → PASS sampai smoke ekstrak; pin peringatan normalisasi ekstrak tidak berubah; sistem-benih tetap lolos gerbang mandiri dengan satu salinan berlabel.
- `python3 tools/check_selfcontained.py --semua --report` → dua sistem domain lama tetap MERAH; keluaran utuhnya ditempel di body PR (tidak diringkas, tidak dikutip angkanya di prosa).
- Uji mutasi lima pemeriksaan cakupan (A: penilaian area master-only pada rujukan; B: cakupan dokumen aktif; C: pemeriksaan salinan berlabel; D: pengecualian bentuk direktori; E: penilaian area master-only pada sumber salinan) — masing-masing dimatikan permanen di salinan repo `/tmp`, injeksi kegagalan MERAH dengan exit 1, lalu dipulihkan.

## Batas yang sengaja tidak dikerjakan

- Tidak menyunting isi `sistem-konten-kreator/` maupun `sistem-presentasi/`; temuan yang tersisa adalah daftar kerja PR B/C.
- Tidak melonggarkan satu pun pemeriksaan untuk dokumen aktif; tidak menambah mode, daftar putih, atau mekanisme paket baru.
- Tidak menutup gerbang apa pun dan tidak melakukan merge.

## Langkah berikutnya

- Pemilik/reviewer menjalankan prompt dari `python3 tools/review_prompt.py --pr <nomor PR>` terhadap PR sesi ini.
- PR B/C: menuntaskan temuan yang tersisa di dua folder sistem (rujukan berkas master tanpa salinan berlabel, rujukan ke diri sendiri berprefiks folder, dan rujukan area master-only yang harus dijadikan provenance tanpa backtick).

## Penutup — PR dibuka

- **PR #27** — `PR A2 (meta v1.11.0): cakupan gerbang folder mandiri — dokumen aktif, sebutan area, area master-only = provenance`, base `main`, **tanpa merge**. Body memuat dua tabrakan aturan (dengan baris keluaran alat versi `main` yang dijalankan ulang di salinan `/tmp`), empat perubahan, tafsir butir 5(iii) yang dinyatakan terbuka, bukti uji mutasi, keluaran langkah 7 UTUH (termasuk seluruh keluaran gerbang `--semua --report` sebagai daftar kerja PR B/C), dan daftar berkas. Jumlah rujukan/dokumen tidak dikutip di prosa body (AT-16).
- **HEAD isi kerja:** `ffcd2c5fd8b28020e98f193b51ad27cd485d04a8` — commit penutup yang memuat entri ini ada di atasnya dan hanya menyentuh berkas log yang tidak dipindai validator, jadi seluruh keluaran bukti di atas tetap berlaku di HEAD akhir.
- Rangkaian verifikasi pada pohon bersih: validator master PASS dengan `WARNINGS: none`; injeksi kegagalan PASS (baris Jumlah disalin dari keluaran alat); pembangun template PASS sampai smoke ekstrak dengan pin peringatan normalisasi ekstrak tidak berubah; gerbang mandiri tetap MERAH pada dua sistem domain lama sebagai keadaan yang dideklarasikan.
- Prompt review dibangkitkan alat: `python3 tools/review_prompt.py --pr 27`, keluarannya ditempel utuh sebagai satu blok berpagar di badan pesan penutup sesi.
- **Status: menunggu review independen L1.** Tidak merge, tidak ada gerbang yang diklaim tertutup.
